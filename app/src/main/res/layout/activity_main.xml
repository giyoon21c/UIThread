<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.android.uithread.MainActivity">


    <Button
        android:text="stop thread"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonStopThread"
        android:layout_below="@+id/buttonThreadStarter"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="135dp" />

    <Button
        android:text="start thread"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonThreadStarter"
        android:layout_below="@+id/textViewThreadCount"
        android:layout_alignLeft="@+id/buttonStopThread"
        android:layout_alignStart="@+id/buttonStopThread"
        android:layout_marginTop="110dp" />

    <TextView
        android:text="Thread Count value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textViewThreadCount"
        android:textSize="25sp"
        android:layout_marginTop="64dp"
        android:layout_alignParentTop="true"
        android:layout_alignLeft="@+id/buttonThreadStarter"
        android:layout_alignStart="@+id/buttonThreadStarter" />

</RelativeLayout>
